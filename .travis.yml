# From https://medium.com/flutter-io/test-flutter-apps-on-travis-3fd5142ecd8c
os:
  - linux
sudo: false
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libstdc++6
      - fonts-droid
before_script:
  - git clone https://github.com/flutter/flutter.git -b beta --depth 1
  - ./flutter/bin/flutter doctor
  - git clone https://github.com/flutter/flutter.git -b dev --depth 1 flutter-dev
  - ./flutter-dev/bin/flutter doctor
  - chmod +x travis_script.sh
script:
  - ./travis_script.sh
cache:
  directories:
    - $HOME/shared/.pub-cache
